# Database Configuration
# For local development (SQLite): Leave DATABASE_URL empty or comment out
# For Render.com (PostgreSQL): Set DATABASE_URL to your Render database URL

# Local development (SQLite) - Works on your computer
# DATABASE_URL=

# Render.com production (PostgreSQL) - Required for persistent data
# DATABASE_URL=postgresql://username:password@host:port/database

# Example Render DATABASE_URL (copy from Render dashboard):
# DATABASE_URL=postgres://user:password@host:5432/database_name

# Server Configuration
HOST=0.0.0.0
PORT=8000
DEBUG=false

# CORS Configuration (comma-separated origins)
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8080
